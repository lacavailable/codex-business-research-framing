# v0.1.0 Baseline Audit

This audit records the verified starting point for the `v0.2.0-beta` development branch. It describes commit `cb8df4c66380e89b05b55bf2ceb95379afd463f3` before v0.2 working-tree changes.

## Recorded baseline

| Check | Verified result |
|---|---|
| Local commit | `cb8df4c66380e89b05b55bf2ceb95379afd463f3` |
| Branch created for v0.2 work | `literature-and-readme-v0.2.0` |
| Test suite | 12 tests passed |
| Tracked repository audit | 82 files passed |
| Local Markdown link check | 59 files passed |
| Benchmark validation | 30 cases; 10 each for OM, IS, and OR; 3 conditions; 90 expected generations; 100 rubric points |
| Installed official Skill validator | `Skill is valid!` |

The commands were run on 2026-07-19 in the repository's Python environment:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe tools\audit_repository.py --tracked
.\.venv\Scripts\python.exe tools\check_links.py
.\.venv\Scripts\python.exe tools\evaluate_benchmark.py validate
.\.venv\Scripts\python.exe C:\Users\16041\.codex\skills\.system\skill-creator\scripts\quick_validate.py skills\frame-business-research-problem
```

## Rubric discrepancy repaired in v0.2

At the baseline commit, `evals/rubrics/business-framing-v1.json` used a 70-point passing score and a 69-point hard-failure cap. The generated Skill reference did not yet exist and the hand-maintained `references/evaluation-rubric.md` instead stated 60 and 59. The v1 JSON has been archived unchanged under `evals/archive/v0.1.0-alpha/` for provenance. Version 2 has one active canonical JSON policy, and its Markdown reference is generated deterministically.

This record does not claim that a baseline language-model evaluation was complete. The v0.1 report explicitly documented its evaluation status; deterministic validation confirmed only the repository structure and declared benchmark inputs at this checkpoint.

Packaging, checksum/package verification, clean-install verification, and the public `v0.1.0-alpha` release assets were not rerun or independently re-downloaded during this baseline-audit pass. They therefore are not recorded as newly verified results here; the repository's existing v0.1 release records remain the provenance for those checks.
